"""
Helper: implemente aqui o carregamento e o cálculo.
"""


def load_and_compute(filepath: str, has_header: bool, column_name: str = ""):
    """Retorna (df_raw, df_frequencia, stats). Implemente e use pandas se quiser."""
    return None, None, {"erro": "Implemente helper.load_and_compute"}
